// The demo lives in examples/shapes.rs so it shows up as a cargo example. This
// binary reuses that exact source (no duplication) so `cargo run` and the web
// build (Trunk `data-bin="juni"`) run the same demo.
include!("../examples/shapes.rs");
